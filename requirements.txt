# ============================================================
# ROK Unified Tool - Python Requirements
# Save this as: requirements.txt
# Install with: pip install -r requirements.txt
# ============================================================

# Core GUI Framework
tkinter-tooltip==0.1.0

# Image Processing & Computer Vision
opencv-python==4.8.1.78
Pillow==10.1.0
numpy==1.24.3

# OCR (Optical Character Recognition)
pytesseract==0.3.10

# Automation & Input Control
pyautogui==0.9.54
keyboard==0.13.5
mouse==0.7.1

# HTTP Requests (for Discord webhooks)
requests==2.31.0
urllib3==2.1.0

# Additional dependencies that might be needed
python-dotenv==1.0.0

# ============================================================
# NOTES:
# ============================================================
# 1. Python 3.8+ recommended (tested on Python 3.9-3.11)
# 2. Tesseract-OCR must be installed separately (see below)
# 3. ADB (Android Debug Bridge) must be installed for emulator control
# 4. Some packages may require Visual C++ redistributables on Windows
# ============================================================